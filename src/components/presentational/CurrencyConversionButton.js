import React from 'react'

const CurrencyConversionButton = () => {
    const conversion = () => {
        
    }
    return (
        <div>
            <button onClick={conversion}>Convert</button> 
        </div>
    )
}

export default CurrencyConversionButton

// make button text dynamic to switch between AUD and USD
